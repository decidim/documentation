= Portées

Les champs d'application sont l'ensemble des territoires ou des sujets qu'une Organisation possède. Une portée peut avoir des portées enfants.

For instance, in the case of the city of Barcelona the scopes correspond to its districts, and the children of a
district are its neighborhoods.
Ils peuvent également avoir des types, de sorte qu'il peut être configuré, par exemple, par des portées thématiques et également territoriales.

La plupart des composants et des espaces de Decidim vous permettent de configurer des Scopes d'une manière ou d'une autre.

image::settings/scopes_list.png[Scopes list]

== Create a scope type

To create a scope type, click the "Add" button in the Scope types tab.

image::settings/scopes_types_new_form.png[New scope type form]

.Admin new scope type form
[cols="20h,10h,~"]
|===
|Champ |Type |Description

|Nom
|Obligatoire
|Nom du type de scope en singulier.

|Pluriel
|Obligatoire
|Nom du type de portée au pluriel.
|===

== Create a scope

To create a scope, click the "Add" button in the Scope tab.

image::settings/scopes_new_form.png[New scope form]

.Admin new scope form
[cols="20h,10h,~"]
|===
|Champ |Type |Description

|Nom
|Obligatoire
|Nom du périmètre.

|Code
|Obligatoire
|Internal identifier of the scope.

|Type de périmètre d'application
|Facultatif
|Which type of scope it's related to. It can be blank.
|===

== Actions

.Actions
[cols="10h,20h,~"]
|===
|Icône |Nom |Définition

|image:icons/action_browse.png[Browse icon]
|Parcourir
|Voir et configurer les portées enfants pour ce périmètre.

|image:icons/action_edit.png[Edit icon]
|Editer
|Modifier le formulaire pour un scope.

|image:icons/action_delete.png[Delete icon]
|Supprimez
|Supprimer ce périmètre.
|===

== Fonctionnalités

Les principales fonctionnalités liées aux portées sont:

. Filtrage
. Intérêts des utilisateurs et newsletters

=== Filtrage

La plupart des espaces et des composants permettent à un administrateur ou à un participant de les associer à une portée.
Cela permet de filtrer par périmètre lors de leur exploration.

image::settings/scopes_filter_assemblies.png[Assemblies scopes filtering]

image::settings/scopes_filter_processes.png[Processes scopes filtering]

Quelques exemples:

* Avec le module Responsabilité, il est donc possible de filtrer les résultats
* Les assemblages et les processus peuvent avoir des portes. Il est utilisé pour filtrer plusieurs assemblages dans la page principale de la liste (/assemblies ou /processes). These can be hidden with the "Enable participatory space filters" in the xref:admin:settings/configuration.adoc[configuration].
* Filtering in the sidebar is also available in some components, where you can control if these filters are shown with the "Scopes enabled" checkbox in the settings. It also enables the scope selector in the resource creation form.

You can also configure a participatory space so the filtering only concerns one scope and its children scopes. Par exemple, imaginons que votre organisation ait 3 portées différentes pour 3 villes, chacune ayant des portées enfants pour les quartiers urbains.
On a participatory space which concerns only one of the cities, you are able to select only this scope and its children so that users do not see the scopes of other cities in the filter options.

=== User interests and newsletters

A participant can choose which scopes they want to follow in its account.

image::settings/account_my_interests.png[Account: my interests]

This is used in the xref:admin:features/social_features/newsletters.adoc[Newsletters], in the "Select recipients to deliver" step.

image::features/newsletters/newsletter_select_recipients.png[Newsletter: select recipients]